FROM python:3.10-slim-bullseye
USER root
ENV DEBIAN_FRONTEND noninteractive
RUN mkdir /autostart
VOLUME /notebooks
RUN apt update && apt upgrade && apt-get install -y conda && conda install -c conda-forge jupyterlab
WORKDIR /autostart
COPY autostart.sh .
EXPOSE 8888
CMD ["/autostart/autostart.sh"]
